/**
 * Editor Services - Document editing service layer
 *
 * Provides action handlers and utilities for document editing operations
 * using docxmlater APIs.
 */

export {
  executeAction,
  isActionAvailable,
  type ActionResult,
  type ActionContext,
} from './EditorActionHandlers';
